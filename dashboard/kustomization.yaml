apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ClusterRolesBinding.yaml
  - ServiceAccount.yaml
configMapGenerator:
- name: users	
  literals:	
    - DEPLOY_ENV="dashboard"
